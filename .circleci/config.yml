version: 2.1
jobs:
  compile:
    docker:
      - image: hseeberger/scala-sbt:8u222_1.3.6_2.12.10
    environment:
      SBT_VERSION: 1.3.6
    working_directory: ~/Proptics

    steps:
      - run:
      - checkout
      - run:
          name: Assembly a JAR package
          command: |
            sbt ";test;compile"

workflows:
  version: 2
  build:
    jobs:
      - compile:
          filters:
            tags:
              ignore: /^v.*/
            branches:
              only: /.*/